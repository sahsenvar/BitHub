<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Seçili ikon -->
    <item android:drawable="@drawable/ic_home_filled" android:color="@color/githubBlue" android:state_checked="true" />
    <!-- Seçilmemiş ikon -->
    <item android:drawable="@drawable/ic_home"  android:color="@color/githubGray1" android:state_checked="false"/>
</selector>